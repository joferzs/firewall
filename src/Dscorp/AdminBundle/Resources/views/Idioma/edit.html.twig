{% extends '::base.html.twig' %} 
{% block title %}Admin Idioma{% endblock %}
{% block contenido %}
<h1>Editar Idioma</h1>

<form action="{{ path('idioma_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <br>
        <a href="{{ path('idioma') }}" class="btn btn-outline btn-back" >Regresar</a>
        <button class="btn btn-outline btn-edit editar-edit" type="submit">Editar</button>
</form>
<form action="{{ path('idioma_delete', { 'id': entity.id }) }}" method="post">
    {{ form_widget(delete_form) }}
    <button type="submit" class="btn btn-outline btn-danger eliminar-edit">Eliminar</button>
</form>

{% endblock %}
