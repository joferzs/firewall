{% extends '::base.html.twig' %} 
{% block title %}Admin Imagenes{% endblock %}
{% block contenido %}
<h1>Agregar nueva Imagenes</h1>

<form action="{{ path('imagenes_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <br>
        <a href="{{ path('imagenes') }}" class="btn btn-outline btn-back" >Regresar</a>
        <button class="btn btn-outline btn-warning" type="reset">Reset</button>
	    <button class="btn btn-outline btn-primary" type="submit">Crear</button>
</form>

{% endblock %}