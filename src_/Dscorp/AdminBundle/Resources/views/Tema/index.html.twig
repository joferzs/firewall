{% extends '::base.html.twig' %} 
{% block title %}Admin Tema{% endblock %}
{% block contenido %}
<h1>Tema</h1>
{% for entityIdioma in entitiesIdioma %}
<table class="table table-striped table-bordered table-hover dataTables-example">
    <thead>
        <tr><th colspan="4"><h4>Idioma: {{entityIdioma.nombreIdioma}}</h4></th></tr>
        <tr>
            <th>Id</th>
            <th>Nombretema</th>
            <th>Nombreidioma</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        {% for entity in entities if entity.idioma == entityIdioma.nombreIdioma %}
            <tr>
                <td><a href="{{ path('tema_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombreTema }}</td>
                <td>{{ entity.idioma }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('tema_show', { 'id': entity.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('tema_edit', { 'id': entity.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
    </tbody>
</table>
    {% endfor %}

<ul>
    <li>
        <a href="{{ path('tema_new') }}">
            Create a new entry
        </a>
    </li>
</ul>

{% endblock %}