{% extends '::base.html.twig' %} 
{% block title %}Admin Idioma{% endblock %}
{% block contenido %}
<h1>Idioma creation</h1>

<form action="{{ path('idioma_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('idioma') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}